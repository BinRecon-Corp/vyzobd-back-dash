# Permissions Matrix Guide

## Overview
The Permissions page (`/admin/permissions`) displays the system permission matrix, detailing resources (`products`, `orders`, `users`, `settings`) and allowed actions (`read`, `create`, `update`, `delete`).
