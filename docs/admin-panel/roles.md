# Role Management Guide

## Overview
The Roles page (`/admin/roles`) provides complete Role-Based Access Control (RBAC) administration.

## Functionality
- Define custom operational roles (e.g., `Order Manager`, `Catalog Manager`, `Customer Support`).
- Attach granular system permissions.
- Delete custom roles (system default roles protected).
