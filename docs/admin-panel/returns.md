# Return Authorization (RMA) Guide

## Overview
The Return Management interface (`/admin/returns`) handles customer return authorization requests (RMA), track returned physical packages, mark received items, and automatically trigger stock restocking into selected warehouses.
