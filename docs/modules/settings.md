# Platform Settings Module

## Overview
Provides centralized configuration for store branding, SEO defaults, SMTP email server settings, analytics keys, security policies, shipping fees, and tax calculations.

## Models
- `Setting`, `BrandingSetting`, `SEOSetting`, `SMTPSetting`, `AnalyticsSetting`, `SecuritySetting`, `ShippingSetting`, `TaxSetting`
