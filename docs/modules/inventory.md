# Inventory & Warehouse Management Module

## Overview
Tracks multi-warehouse inventory levels, stock allocations, safety stock thresholds, and stock adjustment audit trails.

## Features
- Real-time stock reservation during checkout.
- Multi-location warehouse support (`Warehouse` model).
- Stock level updates via `PUT /api/v1/inventory/adjust`.
