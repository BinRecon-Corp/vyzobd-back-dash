# Shipments & Fulfillment Module

## Overview
Manages courier integrations, shipment generation, tracking numbers, and real-time shipment status events.

## Features
- Courier directory (`Courier` model).
- Order fulfillment (`Shipment` and `ShipmentItem` models).
- Tracking timeline updates (`TrackingEvent` model).
